# TODO — מה נשאר לפני העלאה ל-App Store

## ✅ תוקן (ספטמבר 2026)
1. ~~תיקון Bottom Nav שעולה עם המקלדת~~ — היה כבר מתוקן בקוד (`initKeyboardDetection`), רק המסמך היה מיושן.
2. ~~שמירת נתונים ב-localStorage~~ — כבר קיים ועובד במלואו.
3. ~~מערכת רב-לשונית EN/NL/ES/HE~~ — כבר קיימת ועובדת במלואו.
4. ~~Dark/Light mode~~ — כבר קיים ועובד.
5. ~~סרטוני YouTube אמיתיים לכל 30 התרגילים~~ — הושלמו 14 סרטונים חסרים (handstand, muscle-up, front lever, planche, human flag ועוד), כולם אומתו כחיים וניתנים להטמעה.
6. ~~PWA אמיתי~~ — נוסף `manifest.json` סטטי אמיתי, `sw.js` שמבצע caching אמיתי של כל קבצי האפליקציה, ואייקונים אמיתיים ב-PNG (היו קודם SVG inline מזויף).
7. ~~תלות ברשת ל-Google Fonts ו-Chart.js~~ — שניהם "vendored" (הורדו ומוטמעים מקומית תחת `vendor/`) כדי שהאפליקציה תעבוד אופליין אמיתי מהפעם הראשונה.
8. ~~נגישות בסיסית בניווט הראשי~~ — נוספו role/aria-label/ניווט מקלדת ל-5 לשוניות הניווט התחתון.
9. ~~AI אמיתי לניתוח ביצוע~~ — הוחלף ה-`Math.random()` בניתוח Pose Detection אמיתי (TensorFlow.js/MoveNet) שרץ מקומית על פריימים מהמצלמה: ספירת חזרות אמיתית, טווח תנועה אמיתי, מדידת יציבות.
10. ~~עדכון עטיפת Capacitor (iOS/Android)~~ — `app-store-prep.zip` היה בנוי מגרסה ישנה של index.html; עודכן במלואו כולל האייקון האמיתי במקום ה-X הכחול (ברירת מחדל של Capacitor).

## 🔴 עדיין דורש החלטה/פעולה מהמשתמש (לא ניתן להשלים בלי דוד)
1. **חשבון Apple Developer** — צריך חשבון פעיל ($99/שנה) כדי להעלות ל-App Store Connect.
2. **צילומי מסך אמיתיים למכשירי iPhone** (6.9" ו-6.5", לפחות 3 לכל גודל) — נדרש הרצה על מכשיר/סימולטור אמיתי, לא ניתן ליצור מהסביבה הזו.
3. **בדיקה על מכשיר iOS אמיתי** — הרשאת מצלמה, ביצועי ה-AI בזמן אמת, וחוויית משתמש כללית טרם נבדקו על iPhone פיזי או סימולטור Xcode.
4. **החלטה על מדיניות פרטיות מתארחת (URL)** — Apple דורשת קישור פעיל למדיניות פרטיות באינטרנט (לא רק מסך בתוך האפליקציה) — צריך להחליט איפה יתארח (למשל עמוד ב-GitHub Pages).

## 🟡 בינוני (שדרוגי איכות, לא חוסמים הגשה)
5. סקירת נגישות (aria-label/alt) מלאה לכל הקובץ מעבר לניווט הראשי.
6. עדכון אייקוני Android (mipmap בכל הצפיפויות) — כרגע רק אייקון ה-iOS הראשי הוחלף.
7. ביומכניקה ספציפית יותר לכל תרגיל בניתוח ה-AI (כרגע גנרי: ספירת חזרות/טווח/יציבות לכל התרגילים).

## 🟢 עתידי (פיצ'רים מתקדמים, לא נדרשים ל-MVP של App Store)
8. Push Notifications — תזכורות לפני אימון
9. יומן תזונה מתקדם, מדדים גופניים עם גרפים
10. קהילה, שיתוף חברים, מצב "מירוץ", מערכת XP
11. תמיכה ב-Wearables (Fitbit/Apple Watch)
12. מוזיקת רקע

## 📝 הערות למפתח הבא
- הקוד הוא HTML אחד גדול — כל ה-CSS וה-JS inline ב-`index.html`.
- `masa_calisthenics.html` הוא קובץ ישן/לא בשימוש — האפליקציה הפעילה היא `index.html` בלבד.
- תלויות חיצוניות (Chart.js, גופן Heebo, TensorFlow.js/MoveNet) מוטמעות מקומית תחת `vendor/` — אל תחזירו אותן ל-CDN, זה ישבור את מצב האופליין.
- עטיפת ה-iOS/Android נמצאת ב-`app-store-prep.zip` — חובה לסנכרן מחדש (להעתיק את `index.html` העדכני + `manifest.json`/`sw.js`/`icons/`/`vendor/` לתוך `www/`, `ios/App/App/public/`, ו-`android/.../assets/public/`) בכל פעם ש-`index.html` משתנה, לפני build.
